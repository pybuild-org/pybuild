# テンプレートタグ設定

テンプレートで使用する開始タグと終了タグを定義します

```xml
<config name="template">
    <prop name="start-tag" value="" />
    <prop name="end-tag" value="" />
</config>
```

デフォルトでは `{` を開始タグとして、 `}` を終了タグとして使用します