# Template Tag Configuration

Define the start and end tags used by the template

```xml
<config name="template">
    <prop name="start-tag" value="" />
    <prop name="end-tag" value="" />
</config>
```

By default, `{` is used as the start tag and `}` as the end tag.